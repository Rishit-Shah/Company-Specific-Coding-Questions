Given an array = {1,2,4,5,5,6}. Find the required element without looping through all elements.

Soln: Binary Search

int left=0;
int target=some element
int right=array.length-1;
while(left<=right){
int mid= left + (right-left)/2;
if(arr[mid]==target){
return mid;
}
if(arr[mid]<target){
left=mid+1;

}else{
right=mid-1;
}

}
return -1;
